spring:
  application:
    name: configclient
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-service
  profiles:
    active: test
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8081/eureka/,http://localhost:8082/eureka/

